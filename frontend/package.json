{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "prestart": "node scripts/generate-config.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "elm-live src/Main.elm --pushstate --host=127.0.0.1 --port=3001 -- --output=main.js",
    "prebuild": "node scripts/generate-config.js",
    "build": "elm make src/Main.elm --output=main.js --optimize"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "body-parser": "^2.2.0",
    "meow": "^14.0.0",
    "qs": "^6.14.0",
    "trim-newlines": "^5.0.0"
  },
  "devDependencies": {
    "elm": "^0.19.1-6",
    "elm-live": "^4.0.2",  
    "elm-format": "^0.8.7"
  }
}